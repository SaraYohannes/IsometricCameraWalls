using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// INTENT: Watches for Player using Left Mouse Button clicks on Objects in the Scene
//         then passes the reference to other scripts
public class PlayerInteract_LMouseOnScreen_Script : MonoBehaviour
{
    /*
     * NEEDS:
     * - Raycaster
     * - Watch for Left Mouse Button clicks
     * - Get Ref. for Object
     *   - Pass to BoomScript
     *   - If Player right Mouse Button clicks, item is 'deselected' and Ref for Object is cleared
     */ 

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
